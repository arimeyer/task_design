import hours from './hours.js'

// const getHour = date => {
//     const local = date.toLocaleTimeString()
//     const parts = local.split(" ")
//     const amOrPm = parts[1]
//     const hour = parts[0].split(":")[0]
//     const minutes = parts[0].split(":")[1]
//     return ({[`${hour} ${amOrPm}`]: minutes})
// }

// export default items =>  

/**
 * type items {
 *      []task
 * }
 * 
 * type item {
 *      startTime: date
 *      endTime: date
 *      content: string
 * }
 * 
 * to locale time string 6:10:59 AM
 */