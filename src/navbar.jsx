import React from 'react'

const NavBar = () => {
    const styles = {
        container: {
            gridArea: 'nav',
            background: 'red'
        }
    }
    return (
        <div style={styles.container}>
        </div>
    )
}

export default NavBar